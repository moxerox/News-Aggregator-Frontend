const ProfilePage = ({ ...props }) => {
    // TODO: implement profile update + password change
    return (
      <div>
        <h2>Profile Page</h2>

      </div>
    );
  };
  
export default ProfilePage;  